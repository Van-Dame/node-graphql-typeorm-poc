# import DateTime from 'DateTime.graphql'

type Profile {
  id: Int!
  firstName: String!
  lastName: String!
  gender: String!
  photo: String
  createdAt: DateTime!
  updatedAt: DateTime
}

input ProfileInput {
  firstName: String!
  lastName: String!
  gender: String
  photo: String
}
